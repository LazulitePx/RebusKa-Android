package com.example.rebuska.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primarios ──────────────────────────────────────────
val Blue900   = Color(0xFF0D47A1)
val Blue800   = Color(0xFF1565C0)   // Primary
val Blue700   = Color(0xFF2E70D0)   // Primary variant
val Blue400   = Color(0xFF3A7DDE)   // Light accent
val BlueLight = Color(0xFFEEF4FF)   // Fondos suaves
val BlueBorder= Color(0xFFC5D8FF)   // Bordes

// ── Estado / Feedback ──────────────────────────────────
val Gold      = Color(0xFFF9A825)   // Rating stars
val GoldLight = Color(0xFFFFF8E1)   // Disclaimer bg
val GoldBorder= Color(0xFFFFE082)
val Red800    = Color(0xFFC62828)   // Error / Delete
val RedLight  = Color(0xFFFFEBEE)
val Orange    = Color(0xFFF57F17)   // Paused badge

// ── Neutrales ──────────────────────────────────────────
val TextPrimary  = Color(0xFF1A1A2E) // Texto principal
val TextMuted    = Color(0xFF888888) // Texto secundario
val TextDisabled = Color(0xFFBBBBBB)
val Surface      = Color(0xFFF8FAFF) // Surface cards
val Background   = Color(0xFFF2F4F8) // Fondo general app
val OuterBg      = Color(0xFFDDE3EA) // Fondo externo
val White        = Color(0xFFFFFFFF)
val DividerColor = Color(0xFFEEEEEE)

// ── Dark (Frame / Notch) ───────────────────────────────
val DarkFrame    = Color(0xFF1A1A2E)
val DarkFrameMid = Color(0xFF2D2D44)